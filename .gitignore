# Baseline image directories (large binaries + tool with own git repo)
baseline-2.42/seestarOS.img
baseline-2.42/seestarOS.zip
baseline-2.42/rkdeveloptool/
baseline-current/
*.img
*.img.gz
*.zip

# Device filesystem extract (rsync from working scope, large)
s50-fs/

# APK archives (large binaries; keep current APKs out too)
apks/

# Firmware packages (large binaries)
firmware/packages/
firmware/signed/

# Decompiled source (generated; re-run jadx to rebuild)
firmware/decompiled/

# Python virtualenv
analysis/.venv/
**/.venv/

# Python build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Editor
.DS_Store
*.swp

# Experimental / scratch tools (rebuild from docs if needed)
tools/experimental/
tools/custom_firmware/
tools/dhdutil/

# Device-driver binaries — users extract their own via tools/extract_factory_bcmdhd.sh
# or rebuild the patched one via the objcopy command in SEESTAR_WIFI_WEDGE_FIX.md.
firmware/factory/
firmware/experimental/

# Per-run snapshot dirs (prepare_for_upgrade.sh output)
analysis/pre_*_snapshot_*/

# ZWO-copyrighted analysis artifacts (decompiled APKs, extracted binaries,
# ghidra decompilation outputs). The scripts and our written reports are
# checked in; the raw ZWO-derived material is not.
analysis/jadx/
analysis/ghidra_diff/binaries/
analysis/ghidra_diff/out/
analysis/ghidra_diff/out_ids/
analysis/ghidra_diff/proj/
analysis/fw_5.82_failure_dmesg/
